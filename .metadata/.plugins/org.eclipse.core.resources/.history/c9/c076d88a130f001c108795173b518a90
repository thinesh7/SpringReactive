package com.bms.repository;

import org.springframework.data.r2dbc.repository.Query;
import org.springframework.data.repository.reactive.ReactiveCrudRepository;
import com.bms.entities.Customer;

public interface CustomerRepository extends ReactiveCrudRepository<Customer, Long> {

	@Query("SELECT * FROM Customer c WHERE c.customerSSNID =?1 and c.customerMobileNumber=?2")
	public Customer checkCustomerIfAlreadyExist(String customerSSNID, String customerMobileNumber);
}
