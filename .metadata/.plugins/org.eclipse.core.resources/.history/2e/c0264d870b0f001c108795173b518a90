package com.bms.util;

import org.springframework.beans.BeanUtils;
import com.bms.dto.CustomerDTO;
import com.bms.entities.Customer;

public class AppUtils<R, T> {

	public Customer convertCustomerDTOtoCustomerEntity(CustomerDTO dto) {
		Customer
		System.out.println(entity);
		BeanUtils.copyProperties(dto, entity); // Source --> Destination
		return entity;
	}

	public R converCustomerEntityToCustomerDTO(T entity) {
		R dto = (R) new Object();
		BeanUtils.copyProperties(entity, dto); // Source --> Destination
		return dto;
	}

}
